---
import ChevronLeftIcon from 'astro-heroicons/outline/ChevronLeft.astro'
import ChevronRightIcon from 'astro-heroicons/outline/ChevronRight.astro'
import type { Nav } from '../utils/article'

interface Props {
  nav: Nav
}

const { nav } = Astro.props
---

<nav class="flex justify-between items-center py-8 px-2 text-sm">
  {
    nav.prev !== null ? (
      <a
        href={nav.prev.path}
        class="flex gap-1 basis-1/2 items-center justify-start text-start"
      >
        <ChevronLeftIcon class="flex-shrink-0 size-5" />
        <div>{nav.prev.title}</div>
      </a>
    ) : (
      <div />
    )
  }
  {
    nav.next !== null ? (
      <a
        href={nav.next.path}
        class="flex gap-1 basis-1/2 items-center justify-end text-end"
      >
        <div>{nav.next.title}</div>
        <ChevronRightIcon class="flex-shrink-0 size-5" />
      </a>
    ) : (
      <div />
    )
  }
</nav>
